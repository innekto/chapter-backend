const deletedAccountMessage = 'Account was deleted. Do you want to restore?';

const bookMessage = 'Field must contain only Cyrillic or Latin letters';

const passMessage =
  'The password must contain 8 characters, one capital letter and one number, also password should be only Latin letters';

const loginErrorMessage = 'incorrect email or password';

export { deletedAccountMessage, bookMessage, loginErrorMessage, passMessage };
